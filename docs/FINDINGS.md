# The Void — Findings Register

**This is the living list of everything still open.** Unlike the dated audit snapshots, this file is
**updated continuously** — every time something is decided, built, or newly discovered.

## Rules for this file

1. **An item is only resolved when it is written into an authoritative document** — not when it is
   discussed. Mark it `DECIDED` and link where it landed.
2. **Never delete a resolved row.** Strike it and keep the link. The history is why nobody
   re-litigates it.
3. **New findings get added here**, not into a new audit file. The two audits are frozen evidence
   snapshots; this is the register.
4. **`BLOCKS` means work cannot correctly start** — check this column before beginning any unit.
5. **⚠ PROPAGATE IN THE SAME TURN. A row added here is not done until it exists in the work plan.**
   This register is not what a builder reads — `docs/PLAN.md` is. **This failure has happened seven
   times in six days**, in both directions, and each time the next unit would have shipped without a
   known blocker:
   - *2026-08-28* — G29–G34 got `BLOCKS` tags here and reached **no unit** in `PLAN.md`.
   - *2026-08-28* — **A8** was queued here and in `INTERVIEW-PLAN.md` and **nowhere else**.
   - *2026-08-30* — G35–G40 reached the **coverage cells only**; the numbered list and the counts a
     planner reads still described the previous batch.
   - *2026-08-30* — **A9** repeated A8's failure exactly, two days later, *and* carried a `BLOCKS`
     value that would have deadlocked the next unit.
   - *2026-08-31* — the **codex** was `[DECIDED]` twice and reached no work item (D10).
   - *2026-08-31* — the validation round closed six author rounds and left `PLAN.md`'s own
     interview-status table asserting **all six were still OPEN** for a full commit.
   - *2026-08-31* — **G46** reached the coverage cells only; `#0`'s numbered list stopped at G45.

   **The checklist, every time a row is added or a `BLOCKS` value changes:**
   1. Add the id to the **covering unit's `Covers` cell** in `PLAN.md`.
   2. Add it to that unit's **numbered prose list** — the cells and the prose drift apart otherwise,
     and it has happened in both directions.
   3. Update the **counts and id ranges** in `PLAN.md` *and* `PROGRESS.md`. **Count by script, never
     by hand** — this count has been wrong five separate times.
   4. If it is an **author question**, add it to `PLAN.md`'s open-rounds table **and to every item it
     blocks**, and sanity-check the `BLOCKS` value points at the unit that would actually action it.
   5. If the row's principal file is not in the unit's **principal-files list**, add it — that list is
     the unit's territory declaration for parallel-safety.

**Status:** `OPEN` needs a decision · `DECIDED` settled, link to where · `BUG` needs fixing not
deciding · `FIXED` done · `PARKED` deliberately deferred, with a reason.

**Evidence:** `docs/SCOPE-AUDIT.md` (2026-08-25 — content, systems, doc contradictions) ·
`docs/SCOPE-AUDIT-2.md` (2026-08-26 — open decisions, unexamined areas, accidental decisions).

---

## 1. Blocks shipping

| # | Item | Status | Blocks | Notes |
|---|---|---|---|---|
| S1 | ~~Content warning~~ | `DECIDED` | — | **Policy set: shown at the start of EVERY fresh run, not just the first**, plus store page and README. `docs/CONTENT-WARNING.md`. **The words are still the author's to write** |
| S2 | ~~LICENSE / NOTICE / attribution~~ | `DECIDED` | — | **Proprietary game + a THIRD-PARTY file** with Qwen3, Electron, llama.cpp, Kaplay, JetBrains Mono. `package.json` fields still to fill. `docs/SHIPPING.md` |
| S3 | **Generated-asset licence** — commercial redistribution terms for the images recorded nowhere | `OPEN` | ship | And separately for the **music** — art and music terms can differ |
| S4a | ~~Colour is the only state channel~~ | `DECIDED` | — | **A persistent floor-name tag + a narration beat on every descent.** The accent becomes reinforcement, not information. Rule: the accent may never be the only carrier of any state. `UI-DESIGN.md` §11 |
| S4b | ~~Reduced motion~~ | `DECIDED` | — | **One setting; shake off, flash becomes a soft tint, particles reduced, beat timing unchanged. Honours the OS `prefers-reduced-motion` by default.** #6 and #7 must route every motion effect through the flag. `UI-DESIGN.md` §13 |
| S4c | ~~Text size / contrast / screen reader / keyboard~~ | `DECIDED` | — | **Full commitment, including a tested screen-reader pass.** Viable because the game is text-heavy DOM — a blind player could plausibly play all of it. Keyboard must be *completed*, not left half-styled. `UI-DESIGN.md` §15 |
| S5 | ~~Update mechanism~~ | `DECIDED` | — | **The itch app handles updates.** No electron-updater, no publish block. Needs butler and a real version number. `docs/SHIPPING.md` |
| S6 | ~~Age rating~~ | `DECIDED` | — | Self-declare honestly, following `docs/CONTENT-WARNING.md`. `docs/SHIPPING.md` |
| S7 | ~~Privacy statement~~ | `DECIDED` | — | No network calls beyond the model download; say so. `docs/SHIPPING.md` |
| S8 | ~~Telemetry~~ | `DECIDED` | — | **None.** A decision, not an omission. `docs/SHIPPING.md` |
| S9 | ~~itch pricing~~ | `DECIDED` | — | **Free, optional donation.** Store copy still to WRITE — it must state the 2.5 GB download, the GPU requirement, English-only, one difficulty, and carry the content warning |

## 2. Blocks building

| # | Item | Status | Blocks | Notes |
|---|---|---|---|---|
| B1 | ~~Settings / options screen~~ | `DECIDED` | — | **Added to `screens-restyle` (#8) as its own screen**, routed from both title and hub: audio, accessibility, text speed, model tier, display. `UI-DESIGN.md` §12 |
| B2 | ~~Equipment slot set~~ | `DECIDED` → **build work** | **#1 #6 #8** | **Seven: head, body, hand ×2, feet, trinket ×2.** Cut amulet, ring, legs, back. Relics occupy the two trinket slots — wear two. `GAME-DESIGN.md` §14.7. ⚠ **The shipped engine has NINE and no `trinket` slot at all** (`item.ts:49`) — migration is `PLAN.md` #1.10 |
| B3 | ~~Two-handed vs shield vs dual-wield~~ | `DECIDED` | — | **All three: 1H+off-hand, dual-wield, or 2H which blocks the off-hand.** Dual-wield needs its own attack rules and is a real balance surface. `GAME-DESIGN.md` §14.6 |
| B4 | ~~Backpack capacity / weight~~ | `DECIDED` | — | **A fixed slot count, no weight — N = 12** (§22.17, the #2 re-run's starting value; N is now the healing budget too, per §22.6). `GAME-DESIGN.md` §14.6 |
| B5 | ~~Typeface~~ | `DECIDED` | — | **JetBrains Mono** (SIL Open Font Licence, free to bundle). Chosen for legibility at small sizes — 11px condition chips and dense combat logs. Re-check the type scale against it |
| B6 | ~~Turn / initiative model~~ | `DECIDED` | — | **No initiative system.** Fixed round order; Quick/Slow redefined to work without one. No turn queue to render. `GAME-DESIGN.md` §14.8 |
| B7 | ~~`jsdom` vs `node` test environment~~ | **✅ DECIDED 2026-08-31** (§22.18) | — | **`node` default, per-file `jsdom` opt-in** — node proves the no-DOM-in-`src/game` rule by construction; written into #1 change 8 |
| B8 | ~~Min-spec quality gating~~ | `DECIDED` | — | **No degradation — everything always on.** Every player sees the same thing. `UI-DESIGN.md` §16 |
| B9 | **Performance target** | `DECIDED`, one **measurement outstanding** | #7 | **30fps.** But nobody has ever measured the canvas + 4B inference together on the min machine — **do it before #7 ships**, and revisit if 30fps is not achievable. `UI-DESIGN.md` §16 |
| B10 | ~~Model tier~~ | `DECIDED` | — | **4B only; the 1.7B fallback is rejected. MIN SPEC NOW REQUIRES A GPU** — a changed commitment, updated in `CLAUDE.md`. The store page must say so |
| B11 | ~~Consumables & uniques source~~ | `DECIDED` | — | **Consumables from drops + chests; uniques as a rare drop from any enemy.** Accepted cost: with only 4 uniques, most runs see none. `GAME-DESIGN.md` §14.8 |
| B12 | ~~Grace vs cast-down thresholds~~ | **✅ DECIDED 2026-08-31** (§22.16) | #2 | **Grace is GENEROUS: any net-positive weighted ledger earns it; zero or below is cast down.** Author's call against the recommendation; accepted cost recorded (act 5 skipped by many merciful runs). Tuned in #2's re-run |
| B13 | ~~Ending text~~ | `DECIDED` | — | **Authored anchors, narrated specifics.** "Made whole" is the thesis and must be the author's words; the run's specifics are generated. Same shape as the death summary. `GAME-DESIGN.md` §14.10 |
| B14 | **Enemy scaling formula** + **affix list** — both `[OPEN → M8]`, M8 merged unanswered | `OPEN` | #9 | Two affixes still "provisional" in data |
| B15 | ~~Item-icon granularity~~ | **⚠ REVERSED 2026-08-31** — `GAME-DESIGN.md` §22.8: **NO ITEM ICONS SHIP.** The Tibia paperdoll is dropped for text lists (§22.9), which removes the reason icons existed. Cancelled: ~68 assets / 204 images / ~$13.67. *(Kept and struck per rule 2, not deleted.)* Original ruling: | — | ~~**Bespoke, one per item.** Deadlock broken by the slot set landing. **68** items today ≈ 204 images ≈ $13.67 batched — but ⚠ **a permanent commitment: every new item needs art forever.**~~ `ART-BIBLE.md` §13 |
| B16 | ~~Interface furniture~~ | `DECIDED` | — | **No generated interface art**; cursor + loading indicator excepted as *functional marks*. Resolved by the art/interface rule: **art depicts things in the world; the interface that frames them stays flat.** `ART-BIBLE.md` §15 |

## 2b. Reopened by the Hollow ascent (2026-08-27)

The Hollow campaign ends at the surface, which reopens two things that were **deliberately parked**
on the grounds that the game is a descent and never goes up. `docs/WORLD.md` §13.

| # | Item | Status | Blocks | Notes |
|---|---|---|---|---|
| R1 | ~~Absolution above ground~~ | `MOSTLY CLOSED` | — | **The ascent ends at the Undercity's exit — the Hollow never enters the city.** Only enough to be *glimpsed*. Shape decided: stratified by height. `WORLD.md` §14 |
| R2 | ~~House Grandmore as a place~~ | `CLOSED` | — | **Not needed.** Extractions happen in **Memorian infrastructure inside the Rift**, not in a noble seat. `WORLD.md` §14 |
| R3 | ~~The Memorians as a final encounter~~ | `DECIDED` | #11 | **Not a fight.** They do not fight because to them it is not a person. Mirrors and inverts the Warden — one does not fight because it sees you, the other because it cannot. `WORLD.md` §14 |
| R4 | ~~Second prose pass, all five floors~~ | `DECIDED` | — | **Authoring work, not a decision** — folded into #13. The inverted register roughly doubles floor *content* work |
| R5 | ~~Floor hook must be direction-aware~~ | `DECIDED` | — | **Build work, not a decision.** Specified in `PLAN.md` #1.3 and locked in `GAME-DESIGN.md` §17.5 ("built bidirectionally"). *It was wrongly listed as blocking #1, contradicting `PLAN.md`.* |

## 2c. ~~Still needs the author~~ — **ALL SIX CLOSED 2026-08-31** (`GAME-DESIGN.md` §22)

*(Historical preamble — the first two were marked `DONE` in the interview plan while the design
document itself said the author must confirm: the worst failure mode of this system, a question
that looks answered. All six rows below are now `DECIDED`, each linking its §22 ruling.)*

| # | Item | Status | Blocks | Notes |
|---|---|---|---|---|
| A1b | ~~The potion fold-in~~ | **✅ DECIDED 2026-08-31** — `GAME-DESIGN.md` §22.6: **potions FOLD INTO consumables** — no separate resource; healing competes for backpack space. *Rejected: keeping the dedicated resource and merely reducing it.* ⚠ Changes the balance surface, so it lands with the #2 re-run | #2 | §18.4 says outright: *"⚠ Interpretation flagged for the author… if the intent was to keep the separate potion resource and merely reduce it, say so and this section changes."* It also gates the balance re-run |
| B4c | ~~The boss-talk concession cap~~ | **✅ DECIDED 2026-08-31** — `GAME-DESIGN.md` §22.7: **ONE concession per fight.** Talk freely; the boss yields at most once. *Rejected: no cap (would become the dominant strategy) and a per-boss budget (per-boss authoring for a problem one rule solves).* | #6 #11 #12 | §20 says: *"Flagged, with a proposed fix the author may overrule."* Free talk + earnable concessions is an exploit; the proposed cap is one concession per fight |
| **A10** | **May the narrator reference a PREVIOUS RUN? A `[LOCKED]` rule forbids it and a work item requires it** ⭐ added 2026-08-31 | **✅ DECIDED 2026-08-31** — `GAME-DESIGN.md` §22.4: **§12 WINS — the narrator NEVER references a previous run.** Strike §7's narrator clause; `karmaMemory` stays as a SILENT record — ⚠ note it currently has **no consumer at all**: the unlock path reads `RunSummary`, not `karmaMemory` (its only named consumer was the narrator, now forbidden). ~~Whether it is kept for future non-narrated use or deleted is open~~ **ruled at the second sitting: DELETED (§22.14)**. Also drop "its cross-run memory must be read" from §14.2, remove that half of `PLAN.md` #10, and correct the `unlockStore.ts` comment. *Rejected: the haunting — a good idea that lost to a load-bearing locked rule.* | **#10 #12** | **`WORLD.md` §12 `[LOCKED 2026-08-25]`:** *"Death and restart are NOT explained… **the narration never acknowledges a previous one.** Deliberately unexplained rather than undecided — **do not invent a diegetic justification for it later.**"* **`GAME-DESIGN.md` §7 `[DECIDED]`, untagged:** *"the Void remembers in **flavor only**… a light, non-mechanical cross-run memory **lets the narrator faintly reference who you were before** — haunting, never a power source."* **These cannot both be followed.** ⚠ **This is not dead prose — it is a live build instruction with state already shipping:** `RunMemory` persists `endingType`, `spares` and `bossKills` per completed run; `unlockStore.ts:32` comments *"(the M11 narrator reads it)"*; §14.2 says *"its cross-run memory **must be read** — it is currently written and never consumed"*; and **`PLAN.md` #10 lists reading it as work to do.** The only reason the locked rule is not violated today is that the wiring is unfinished — **#10 exists to finish it.** **Precedence says `WORLD.md` wins** (rule 2 on fiction; `[LOCKED]` beats untagged `[DECIDED]`) — but **I have NOT applied it**, because unlike the other precedence calls this would *delete planned functionality*, and the haunting is a good idea the author may want. **Two options:** strike §7's narrator clause and keep `karmaMemory` as a non-narrated record (then drop the requirement from §14.2, #10 and the code comment) — or **write an explicit carve-out into `WORLD.md` §12**, which is the only document that can grant one |
| **A9** | **Is a nameable sanity mechanic allowed? Two DECIDED rulings conflict** ⭐ added 2026-08-30 | **✅ DECIDED 2026-08-31** — `GAME-DESIGN.md` §22.3: **§13 WINS — rename `insanity`** (the name, chosen at the second sitting: **`Static`**, §22.13). The theme stays unpointable. Rename the condition as §21.1 already renamed `wise/fool` → `Lucid/Clouded` — a name for the STATE, not a diagnosis. "Maddening Gaze" and the "Clarity Draught" follow. *Rejected: keeping the id and carving out §13.* | **#1 #13** | **§13 `[DECIDED]`:** *"The psychosis theme is **subtle and ambiguous — never named, never metered**… the player should feel it and **be unable to point at the 'sanity mechanic', because there isn't one.**"* `ART-BIBLE.md`: *"**Never signpost mental illness.**"* **But §21.1 `[DECIDED 2026-08-27]`, the later ruling,** canonises `insanity` in its restated condition list — and the game ships a status chip named **"Insanity"**, a skill called **"Maddening Gaze"**, and a **"Clarity Draught"** whose entire function is to cure it. That is a pointable sanity mechanic with a cure item. **This is not code drifting from design — it is two design rulings that cannot both hold.** If §13 wins, `insanity` needs renaming (the same treatment §21.1 gave `wise/fool` → `Lucid/Clouded`) and the code follows. If §21.1 wins, §13 needs an explicit carve-out — otherwise every future writer hits this again |
| **A8** | **The endings' voice, and whether the player has a name** ⭐ added 2026-08-28 | **✅ DECIDED 2026-08-31** — `GAME-DESIGN.md` §22.1–22.2: **endings speak in SECOND PERSON and never by name** — the shipped third-person anchors are the defect; `WORLD.md` already renders both correctly. **AND the player DOES type a name** — it is a player-facing label (sheet, save slots, interface) that **the narrator never uses.** The two divide cleanly by surface. | **#13 #14** | `WORLD.md` §8 `[LOCKED]` says *"second person, inside, only"* — but `story.json`'s ending anchors are **third person and name you** (*"{playerName} is judged worthy…"*). WORLD renders the same endings correctly in second person, so the data is the defect — **unless** the archive filing you from outside is a deliberate exception, which is thematically strong and is **nowhere on paper**. Separately, **no document states the player has a name at all**, while the UI mockup shows one and the engine interpolates `{playerName}`. Queued as `INTERVIEW-PLAN.md` **A8** |
| **G15** | **Karma inputs & verdict weighting** ⭐ added 2026-08-28 | **✅ DECIDED 2026-08-31** — `GAME-DESIGN.md` §22.5: **WIRE the four missing karma actions** — `leaveOffering`, `honorDead`, `embraceWhisper`, `seeThroughIllusion`. The four-axis design ships as designed. *Rejected: re-weighting onto the two axes that work, which would have quietly halved the verdict.* Sequencing: `seeThroughIllusion` lands with floor 2's illusions, which do not exist yet. | **#2 #14** | Half the karma model never fires; `reverenceDesecration` (heaviest verdict axis, weight 3) can only move toward CAST-DOWN; `clarityDelusion` is permanently 0; the grace deal pool is unreachable. **Wire the four missing positive actions, or re-weight onto axes that move?** Queued as `INTERVIEW-PLAN.md` **A7**. *It sat in §4 as a "bug" and in no author queue — exactly the failure this section exists to catch* |

## 2d. Tracked as build work (not author questions)

*These two were briefly logged in §2c by mistake — they need building, not deciding.*

| # | Item | Status | Blocks | Notes |
|---|---|---|---|---|
| B17 | ~~The draft reversal is in no work plan~~ | `TRACKED` — build work | #1 | **Now `PLAN.md` #1.7.** Stats left the draft, per-level allowance, **max level 20** (§19.5) — `draft.ts` still has `CATEGORY_WEIGHTS.stat` and **no level cap exists in `src/`**. M9 demoted to 🔶 |
| B18 | ~~`#6` has two unrecorded requirements~~ | `TRACKED` — build work | #6 | **Now recorded under `PLAN.md` #6.** Audio hooks on every beat (`ART-BIBLE.md` §10, BLOCKING) and a free-text input for boss talk (§20) |

## 3. Product areas never discussed

| # | Item | Status | Notes |
|---|---|---|---|
| N1 | ~~Onboarding / tutorial~~ | `DECIDED` — **⚠ the codex half was CUT 2026-08-31** (§22.11); tooltips only | **Tooltips on everything.** ~~+ a codex that fills in as you go~~ No scripted tutorial. Depends on the description fields from #1. Karma stays exempt and unreadable. `UI-DESIGN.md` §14 |
| N2 | ~~Difficulty / assist options~~ | `DECIDED` | **One difficulty. No modes, no assists**, stated up front on the store page. The unlock system is the accessibility ramp. Accepted cost recorded: some players bounce permanently. `GAME-DESIGN.md` §14.5 |
| N3 | ~~Save slots~~ | `DECIDED` | **Multiple named slots, moved to a real file on disk.** ⚠ Mitigation required: slots are for *parallel* runs, not rewinding — autosave overwrites its own slot, no manual save, no reload. Otherwise save-scumming guts permadeath. `UI-DESIGN.md` §14 |
| N4 | ~~Error handling~~ | `DECIDED` | **Tell the player, retry, and recover.** Nothing silent. Includes a **backup copy of the unlock store** — which is the fix for G3. `UI-DESIGN.md` §14 |
| N5 | ~~Window management~~ | `DECIDED` | **Min size, resizable, fullscreen toggle, remembered bounds, DPI-aware.** The minimum is the layout's design target. `UI-DESIGN.md` §14 |
| N6 | ~~Input model~~ | `DECIDED` | **Mouse is primary and hover is now load-bearing** (tooltips). Keyboard navigation must be **complete rather than half-supported**. Gamepad out of scope. `UI-DESIGN.md` §14 |
| N7 | ~~External playtest / QA~~ | `DECIDED` | **A small trusted group, 5–10 people, before any public release.** The subject matter especially needs trusted people rather than strangers. `ART-BIBLE.md` §14 |
| N8 | **Localisation** | `PARKED` | English-only is right for a 4B English narrator. State it on the store page |

## 4. Bugs — fix, don't decide

> **47 rows** (G1–G47; no G38; plus the lettered G11b), counted by script 2026-08-31:
> **41 `BUG`** — **ALL 41 with a fix specified** (G2's landed §22.15, second sitting) · **3
> `DECIDED`** (G7, G9, G10)
> · **2 author-ruled build items** (G15 → #10a, wire the karma actions · G32 → #0a, wire
> `proficiency`) · **1 `FIXED`** (G44, 2026-08-31).
>
> ⚠ **There is no `G38`.** The id was never used — pass 9A produced five new defects (G35, G36, G37,
> G39, G40), and the G6 *escalation* was miscounted as a sixth. **Do not create a G38 to fill the
> gap** — leave it unused so this note stays legible.
>
> **Do not derive the row count from the highest id.** Two offsets cancel: `G38` is missing (−1) and
> `G11b` is a lettered row with no number of its own (+1), so today the count happens to **equal** the
> highest id. That equality is a coincidence of the current data, not a rule — **count the rows.**
> *(An earlier version of this clause asserted "one higher than the highest id", which was already
> false on the day it was written: it accounted for the missing G38 and never mentioned G11b.)*
> - ~~`G2` — the one `BUG` row with no fix designed yet~~ **✅ designed §22.15 → #0c.**
>
> *(**Counted by script**, 2026-08-31. This line has been wrong six times — "eight", "twenty /
> eighteen specified", "twenty-six", "thirty-two", and once when a correction updated the headline
> and left the explanatory note beneath it condemning the new, correct figure as a miscount.
> **Recount rather than quote it — and when you recount, update every clause in this block, not just
> the number.**)*

| # | Item | Status | Severity | Blocks | Notes |
|---|---|---|---|---|---|
| G1 | **Quitting mid-run voids all unlock progress** | `BUG` — **fix NOW specified, see G19** | ⛔ severe |  #0c  | Resume never rebuilds `runSummary`/`runSeed`. Earn an unlock, quit, return, win — you get nothing. **The whole meta-progression pillar.** ⭐ **Pass 6A found the root cause and the fix** (2026-08-28): `persist.ts` stores only `{state, memory}`, so persist `runSummary` + `runSeed` in the envelope and restore on resume. **See G19 for the measured reproduction** |
| G2 | **Winning leaves a resumable save** | `BUG` — **✅ fix DESIGNED 2026-08-31 (§22.15)** | ⛔ | #0c | `ending` maps to `continue`, not `game-over`. Relaunch after victory: *"A descent lies unfinished."* |
| G3 | **Corrupt unlock store wipes everything silently** | `BUG` — **fix decided** | ⛔ |  #0c  | Keep a **backup copy** and restore from it; if that fails, say exactly what was lost. Also moves to a real file with the saves (N3) |
| G4 | **You can flee the floor-4 Warden** | `BUG` — **fix decided** | ⛔ |  #0a  | **Bosses cannot be fled at all**; Smoke Vial explicitly fails against them. UI must explain why, not just hide the button. `GAME-DESIGN.md` §14.9 |
| G5 | **"Abandon the descent" — one click, no confirmation** | `BUG` — **fix decided** | high |  #8  | **Confirm on abandon + slot overwrite, and MOVE it out of the top group** (`GAME-DESIGN.md` §19.4). Never confirm ordinary combat actions |
| G6 | **Log written inside the install dir, uncapped — and in a PACKAGED build it is SILENTLY DEAD** ⬆ *escalated 2026-08-30 by pass 9A* | `BUG` — **fix decided** | ⛔ *(was: high)* |  #14  | Move to user-data, cap and rotate. `docs/SHIPPING.md`. ⚠ **Escalated 2026-08-30 — this is worse than "misplaced".** `log.mjs` builds `LOG_DIR` from `__dirname`, and `electron-builder` packs `electron/**` **into the asar**. So in a shipped build the path is `…/resources/app.asar/logs`, `mkdirSync` throws inside the read-only archive, the `catch` sets `stream = null`, and **every `fileLog` call becomes a silent no-op.** The uncaught-exception handlers, every forwarded renderer entry, and the whole debug-overlay-to-file pipeline write **nothing** — while `main.mjs` still prints "logging to &lt;path&gt;" so it looks fine. **It works in dev only because `__dirname` is the real repo folder, which is exactly why this survived nine rounds.** So the shipped game has no crash diagnostics at all. **Fix:** mirror `model-path.mjs` — export `configureLogDir(dir)` and call it from `app.whenReady()` with `app.getPath('userData')` |
| G7 | **Equip bypasses the engine** | `DECIDED` → task #1 | ⛔ |  #1  | Balance report is fiction until fixed |
| G8 | **Run seed is wall-clock, never shown or stored** | `BUG` — **fix decided** | medium |  #1 #8  | **Shown, stored and enterable** (`GAME-DESIGN.md` §19.3). Unlocks reproducible tester bug reports |
| G9 | ~~Floor length inherited from the Java port~~ | `DECIDED` | medium |  #2  | **Measure it in the balance re-run and tune the XP curve to the 45–90 min target.** The sim already plays full runs; make it report encounters and minutes per floor, and the grace-vs-damnation asymmetry. `GAME-DESIGN.md` §14.11 |
| G10 | ~~Death has no run-summary screen~~ | `DECIDED` | medium |  #6  | **A run summary written by the narrator** — reached, killed by, your build, unlocked, plus the Void's account. The one place karma is *felt* without being metered. `GAME-DESIGN.md` §14.9 |

### Found 2026-08-28 by discrepancy pass 5B — all six verified by running the real engine

> **These are the most serious defects found in the whole audit.** All six pass the full 1029-test
> suite and a clean typecheck. Each must go through the **agentic pipeline** (`CLAUDE.md`) — none may
> be hand-patched in the main checkout.

| # | Item | Status | Severity | Blocks | Notes |
|---|---|---|---|---|---|
| G11 | **NO item the player finds can EVER be equipped** | `BUG` — **fix specified** | ⛔⛔ **worst in the project** |  #0a  | `rarityGen.ts:116` stamps `defId: 'gen:<rarity>:<slot>'`, but `equipment.ts:131` `canEquip` and `:151` slot inference resolve by **`defId` only** (`resolveGearDef`), which misses every catalog — so it returns `undefined`. Measured: **300 seeds × acts 1–4 + chests → >100 drops, 0 equippable.** Every victory drop, chest and deal reward is inert; the player fights all five Acts in starting gear. The UI renders an **Equip button that silently does nothing**. **Fix:** use `resolveInstanceDef` in `canEquip`, and `resolveInstanceDef(item)?.slot ?? slotForDef(item.defId)` for inference. ⚠ **This invalidates the M15 balance report** — it assumed loot was merely unequipped *by sim policy*, not un-equippable in principle |
| G11b | **Two tests appear to cover G11 and do not** | `BUG` — **fix specified** | ⛔ |  #0a  | `rarityGen.test.ts:77` is *named* "…resolves for equipping" but only calls `resolveInstanceDef`, never `equip()`/`canEquip()`. `equipCascade.test.ts:26-32` writes straight into `inventory.slots.ring`, **bypassing `equip()` entirely** — so the whole M6 cascade suite proves rolled gear works *once equipped* and never that it *can be*. Textbook circular testing (`PRINCIPLES.md` §A3). **Fix:** add a test that calls `equip()` on a real `generateItem` output |
| G12 | **`advantageDisadvantage` is a write-only latch — every boss is fought at the wrong odds** | `BUG` — **fix specified** | ⛔ |  #0a  | `battle.ts:253` writes the field **only when non-zero**, so nothing ever restores it to 0. `encounter.ts:84` stamps `+1` on every random encounter and `game.ts:628-651` persists it to `state.player` — so **every floor boss and the final Hollow is fought at advantage**. In the other direction, a `fracture` (100 turns, "needs a rest" — but `resolveRestDecision` never clears conditions) leaves it stuck at `-1`: verified on seed 1, the first boss opened at **-1** from an unrelated floor-1 Ganger skill. **Boss difficulty swings ±5 to-hit on leftover state.** **Fix:** stop storing it — compute per-round in `resolvePlayerTurn`. Stopgap: write unconditionally + reset to 0 in `createBattle` |
| G13 | **ELEVEN event kinds have no narration case — incl. the player's own skill casts** ~~(was: "four beats render blank")~~ | `BUG` — **fix specified** | ⛔ |  #0b  | `narrate.ts:23` `describeEvent` ends `default: return ''`, so a missing case is silent instead of a type error. No case exists for `boss-encounter`, `verdict`, `spared`, `draft-picked`, `draft-offer` — so `buildNarrationPrompt` returns `null` and `desktop/game.ts:174` leaves the pane empty. Silent today: **every act's boss reveal**, **the act-4 karma reckoning** (the single payoff of the whole karma system), **the entire mercy/sparing path**, and every level-up pick. **Fix:** add the cases and replace `default` with an exhaustiveness check (`const _never: never = e`) so the next new event kind fails the build instead of going quiet. ⚠ **CORRECTED 2026-08-31 by pass 15A — THIS ROW'S ENUMERATION WAS INCOMPLETE AND ITS HEADLINE SYMPTOM IS WRONG FOR MOST CASES.** Six more kinds have no case, including **`skill-cast` — the player's own core class action.** Measured over 400 runs / 170,491 steps: **5,727 steps contain a player skill cast, and 100% of them reach the model with no fact that the player acted.** Only 512 go blank; **the other 5,215 produce narration that MISATTRIBUTES the beat** — and in **1,395** of them the only named skill in the prompt is the **enemy's** identically-named one, so the model will credit the player's biggest hit to the foe. *(Enforcer, seed 1, step 139: the player casts Heavy Strike for 10; the facts read "The enemy unleashes Heavy Strike… The enemy connect for 3 harm.")* Also uncased: `boss-minion-damage` (2,499 steps — **346 with no damage fact at all**, so the player loses HP and nothing says so), `lifesteal` (986), `self-sacrifice` (848), `boss-summon` (541), `boss-adapt` (260). **Anyone implementing this row from its old five-item list ships all of it** |
| G14 | **37 of 38 authored relics/uniques/consumables have no acquisition path** | `BUG` — **fix specified** | ⛔ |  #0c #9  | Traced every `ItemInstance` construction site: generated `gen:*` ids, one hard-coded `mirror-shard` in `deals.json`, starting gear, save migration. **Nothing else.** So `consumableOptions` (`view-model.ts:96`) always returns `[]` — the "Use item" picker never renders and `useConsumable`/`consumable.ts` are unreachable in real play. Every relic effect in `relicEffects.ts` is dead (relics only work equipped; no relic ever enters inventory). The two mastery feats that "grant" relics write `UnlockStore.relics`/`.skills`, and **`snapshotUnlocks` reads neither**. **Fix:** add a weighted catalog branch to `rollLootDrop`/`rollChestLoot`, and read `store.relics`/`.skills` in `snapshotUnlocks`. *(Tracked as task #9.)* |
| G15 | **Half the karma model is inert; the heaviest verdict axis can only move one way** | ✅ **AUTHOR RULED 2026-08-31 (§22.5): WIRE the four missing actions** — build work, now `PLAN.md` **#10a**. ~~`BUG` — needs a design call~~ | ⛔ |  — *(moved to §2c)*  | Of 8 declared karma actions only 4 fire. `leaveOffering`, `honorDead`, `embraceWhisper`, `seeThroughIllusion` appear **nowhere outside `karma.ts`**. Consequences: `reverenceDesecration` is touched only by `desecrateShrine: -2`, so it starts at 0 and can **only ever be ≤ 0** — yet it carries `GATE_WEIGHTS = 3`, the **heaviest** axis in `computeVerdict`, so the axis the reckoning weights most can only push toward CAST-DOWN, never GRACE. `clarityDelusion` is **permanently 0** (a fully dead axis), so "The Delusion" can never be the act-3 boss. `selectPool`'s grace branch needs `reverence >= 3` — **unreachable**, making the entire `grace` deal pool dead content *and* `mirror-shard` unobtainable. The four-axis verdict reduces to `mercyCruelty + restraintGreed >= 1`. **⚠ Fix needs the author** — wire the positive actions to real inputs, or re-weight onto axes that can move and record the deferral. **Blocks split (2026-08-31): the three independently-wirable actions gate #2; `seeThroughIllusion` is delivered *inside* #2, since it needs floor-2 illusions that #2 builds** |
| G16 | **A third dead stat twist, mislabelled** | `BUG` — **fix specified 2026-08-28** | low |  #0a  | `statEffects.ts:198` `dealQualityTwist` is a no-op still commented *"no-op until M7"* — but **M7 shipped**. Same family as the two already tracked (`initiativeOrderTwist`, `illusionSightTwist`). **Fix:** either implement it (CHA shifting deal quality, per §16) or **delete the function and its call sites** — nothing depends on it, so deletion is the default. What is not acceptable is leaving a no-op behind a comment claiming it is pending a milestone that shipped |

### Found 2026-08-30/31 by discrepancy passes 10A / 11A / 11B / 12A / 15A — the only findings in otherwise NIL audits

| # | Item | Status | Severity | Blocks | Notes |
|---|---|---|---|---|---|
| G46 | **The engine stamps second-person text on ENEMY events — the enemy's bleed reads "Your skin is ruptured!"** | `BUG` — **fix specified** | medium *(latent behind G18)* | #0c | `condition.ts:311/374/508` push a **subject-agnostic second-person `text`** field (`'You caught on fire!'`, `'Your skin is ruptured!'`, `'Venom courses through you.'`) onto events tagged with whatever `subject` the tick is for. `format.ts:46` then does `if (e.text) return e.text;` — **short-circuiting its own correct subject-aware template** two lines below. **Reproduced:** Scavver seed 1 step 10 emits `{kind:'condition-onset', subject:'enemy', text:'Your skin is ruptured!'}` and `formatEvent` returns exactly that, for the enemy. Over 250 runs: **676 enemy-subject bleed onsets, 172 enemy-subject poison onsets.** ⚠ **Latent — `format.ts` is dead code (G18), so this goes live the moment G18's one-line log wiring lands.** **Covered by no existing row:** C7 fixes display names, G18 wires the log, C10 fixes `narrate.ts` — none touches `condition.ts`. **Fix:** delete the three `text:` fields (the correct template already exists) or make them subject-aware. *(Same file, same latency, lower value: `format.ts:71/73/77` use plural verbs with the singular "The enemy" — "The enemy **succumb** to fracture" — the same family as C10's `suffer(s)`.)* |
| G47 | **The narrator speaks the player's NAME — an engine-side voice break the content pass cannot fix** ⭐ added 2026-08-31 | `BUG` — **fix specified** | high | #0b | §22.1 ruled the narration never uses the name — and `narrate.ts:28` ships *"You are ${e.name}, a ${e.classId}, at the threshold of the descent."*, a fact line entering the opening prompt and the first five beats of story memory (`MAX_REMEMBERED_BEATS = 5`), fed by `GameEvent.player-created.name` (`game.ts:291`). **Not authored text, so #13 never touches it** — which is why it gets its own row beside C12. **Fix:** drop the name from the fact line; label surfaces keep it (§22.2). ⚠ `story.test.ts:24`, `:66` and `:72-73` assert the token exists and must change with the fix |
| G44 | ✅ **FIXED 2026-08-31** — the `$comment` key is deleted and the config validates (§22.12). ~~**`npm run desktop:pack` HAS NEVER ONCE SUCCEEDED — the config is rejected by its own schema**~~ | `FIXED` 2026-08-31 (§22.12) | ~~⛔⛔ blocks shipping~~ **resolved** | **#14** | `electron-builder.json:2` carries a **`$comment`** key. `app-builder-lib/scheme.json` sets `"additionalProperties": false` at the root and whitelists exactly one `$`-prefixed key — **`$schema`**. `Packager.build` runs `validateConfiguration` **unconditionally, before any packaging work**, so the run dies on first touch: *"configuration has an unknown property '$comment'"*, `failedTask=build`, exit 1. **This is the only command that produces the itch.io build.** ⚠ **It has never worked:** `electron-builder.json` has a **single commit**, and `$comment` is on line 2 of that first version — every `desktop:pack` since the file was created has failed. *(Independently re-verified: the key is present, the file has one commit, and the schema permits only `$schema`.)* **Why nine rounds missed it:** the gate *before* it is green — `npm run build` (`tsc --noEmit && vite build`) passes cleanly — so the failure only shows on a command nobody runs headlessly. Bitterly, `SCOPE-AUDIT.md` §7.3 **quotes this exact line** while auditing the packaging stub, listing everything *missing* on the assumption that what is there works. **Fix:** delete the line (config change, one line). ⚠ **Verify END-TO-END, not just past validation** — `directories.output` defaults to `dist`, the same directory Vite builds into, and `files` includes `dist/**`, so a *second* pack may try to include the first pack's `win-unpacked` inside the asar. ⭐ **Pass 13A applied the fix in a scratch copy and packed:** app packaging **succeeds**, and the produced `app.asar` is correct — `files` delivers exactly what is needed, and `asarUnpack` correctly places `node-llama-cpp` and all five Windows binary variants. **The GPU probe and `getLlama` both work from inside the packaged asar** (`gpu=vulkan`, both devices enumerated), so the packaged inference path is sound. ⚠ **BUT the pack then fails at `winCodeSign` extraction — *"Cannot create symbolic link: A required privilege is not held"*. That is a machine setting, not a repo defect: this Windows box needs Developer Mode enabled (or an elevated shell) to finish a real pack.** The one-line fix is necessary but not sufficient on this machine |
| G45 | **`step` throws on a type-legal non-integer `draft-pick` index, breaking its own totality contract** | `BUG` — **fix specified** | low | #0a | `game.ts:388` guards only the two bounds, so `1.5`, `0.5`, `2.5` and `NaN` all pass, `offers[1.5]` is `undefined`, the `!` assertion hides it, and `applyDraftOption` dereferences `option.kind`. Contradicts the module contract at `game.ts:230`: *"the reducer is total."* **Reproduced:** `index=0/-1/3/Infinity/1e21` → clean; **`1.5`, `0.5`, `2.5`, `NaN` → `TypeError: Cannot read properties of undefined`.** The sibling consumable path is already safe against the identical inputs. ⚠ **Honest caveat: no current caller produces this** — the renderer builds indices from a loop — so it is a contract violation through the engine API, not a live player bug. Round 10's 420,868 totality probes used integer indices only, which is why it survived. **Fix:** `if (!Number.isInteger(index) \|\| index < 0 \|\| index >= phase.offers.length) return noop;` |
| G43 | **FLOOR 5'S ENTIRE ENCOUNTER LAYER IS UNREACHABLE — 21% of the enemy roster can never be met** | `BUG` — **fix specified** | ⛔⛔ | #0a *(+ a balance number → #2)* | `game.ts:411` hard-wires the Hollow to floor **entry**: `act-intro` with `newAct === 5` returns the boss battle directly, where every other act returns `{ kind: 'main-menu' }`. **`main-menu` is the only phase that calls `continueJourney`, which is the only caller of `buildRandomBattle`, `buildChestLoot` and `selectLore`** — so on the True Void there are **no random battles, no chests, no rests, no sacrifice-deals and no lore.** Acts 1–3 gate their boss behind `shouldAdvance` *inside* the hub loop, so the floor is played first; act 5 alone skips the floor. And `shouldAdvance(5, …)` is unconditionally `false`, so no gate could ever start it from the hub either. There is no other door: `advanceAct` is the only writer of `act = 5` and always lands on `act-outro`; fleeing needs `canFlee` (explicitly false) and sparing needs `karmaWeighted` (forced false for bosses). **Measured: an exhaustive input-space walk over 418 act-5 entries — every `GameInput` variant, every skill id, every backpack index — ~17.7 million probed transitions produced `main-menu at act 5: 0` and `non-final battle at act 5: 0`.** A 150-run chained campaign reached 5/5, 4/4, 6/6 and 4/4 distinct families on acts 1–4 and **0 of 5 on act 5**, while the unlock store had already granted every act-5 family. **Dead as a result:** 5 of 24 families (`demons`, `voidHorrors`, `theUnmade`, `echoesOfYou`, `theHollowed`), 5 of the 13 bespoke name tables, and the **`reach-act-5` feat, which grants families that unlock nothing playable.** `createBattle`'s `canFlee: act !== 5` is dead code. **This is a defect, not scope:** §8's floor table gives floor 5 an Enemies column and a signature mechanic exactly like floors 1–4, and `ROADMAP.md` requires each floor to *"**end in** a distinct boss"*. **Fix:** move the Hollow from floor entry to a floor gate — add a floor-5 XP threshold, delete the `newAct === 5` branch so `act-intro` always returns to the hub, and build the Hollow in `continueJourney` when the gate opens. ⚠ **The threshold is a balance number**, so it feeds #2's re-run. *(Side effect: this also removes G39's soft-lock, since a fled Hollow becomes re-fightable.)* |
| G42 | **The ENDING prose is erased by the click that follows it — the run's last screen is blank** | `BUG` — **fix specified** | high | #0b | Found by pass 11B's player-sequence traversal, and **below the bar for its own territory** (it is not a document contradiction) — logged here because it is a real player-visible defect. `game.ts:451` deliberately emits **no events** on the terminal step, *"so the run's final event stays the `ending` event"* — but `narrate()` **clears `narrationEl` before** discovering the prompt is `null`. So the player reads the ASCENSION or DAMNATION text, presses Continue, and lands on an **empty pane with a lone "Descend again"**. The last thing a completed run shows you is nothing. Same mechanism as G13/G21 but at a beat **neither of them covers**. **Fix:** in `narrate()`, only clear the pane once a prompt is confirmed — or hold the terminal text through the final step |
| G41 | **`npm run desktop` orphans the Vite server — every playtest after the first silently serves STALE CODE** | `BUG` — **fix specified** | high *(dev only)* | — *(blocks no unit; `scripts/desktop-dev.mjs`)* | On Windows the script spawns with `shell: true`, so the direct child is `cmd.exe` → `npx.cmd` → `node vite`. `ChildProcess.kill()` is `TerminateProcess` against **that one handle**, and there is no job object, so the grandchild is orphaned and keeps port 5173. **Reproduced:** after `kill()`, `SERVING AFTER kill(): true`. On the next launch the new Vite exits **code 1** (`--strictPort`, port in use) — but `waitForServer()` **succeeds against the orphan**, so Electron starts and the game runs against **the previous session's dev server.** ⚠ **Consequence for the author: quit the game once, and every later `npm run desktop` serves the code from your first session. Edits appear not to take effect, and the terminal error looks unrelated.** **Fix:** drop the shell wrapper — spawn `process.execPath` against `node_modules/vite/bin/vite.js` so `kill()` hits Vite itself (verified to release the port). *Cannot affect a packaged build, which never runs this script.* |

### Found 2026-08-30 by discrepancy pass 9A — all **five** reproduced by running the real engine

*(G35, G36, G37, G39, G40. **No G38** — the sixth item that round was the **escalation of G6**, not a
new defect, and it was briefly miscounted as one.)*

| # | Item | Status | Severity | Blocks | Notes |
|---|---|---|---|---|---|
| G35 | **Taking the "cheaper" upgrade twice drives a skill to ZERO charge cost — unlimited free casts** | `BUG` — **fix specified** | ⛔⛔ | #0a | `draft.ts:86` tests `templateApplies` against the **base** `SKILLS[skillId]`, not the player's already-upgraded skill, and the `taken` exclusion is scoped to a single draft. So `cheaper` is offered again on a later level-up, `chargeDelta` sums to −2, and `resolveSkill` clamps to **0**. The cast guard is `charges < effectiveCost` → `0 < 0` is false, and `useSkill` spends 0. **Measured: 274 of 300 seeds reached a 0-cost skill** under a "take cheaper when offered" policy — the player then casts a damage-plus-momentum skill **every round forever at zero charges.** Reachable on every class with a cost-2 skill. **Fix:** resolve against the player — `resolveSkill(player, skillId)` — which `draft.ts` already imports |
| G36 | **Rejected actions still fire the boss's per-round mechanic** | `BUG` — **fix specified** | ⛔ | #0a | `game.ts:617` guards on `status === 'ongoing'`, but `resolveRound` returns `'ongoing'` for six **no-op rejections** too (`escape-impossible`, `potion-unavailable`/`-blocked`, `cast-unavailable`, `consumable-unavailable`, `spare-unavailable`). None resolves a round; `bossPostRound` runs anyway. **Measured:** the Run button is rendered unconditionally in every battle, and bosses set `canFlee: false`, so **9 rejected "Run" presses against the Act-1 Kingpin cost 11 HP** to summoned minions. Worse on the Act-2 Reflection: **three *rejected* casts burn its once-per-battle adapt**, leaving the player permanently disadvantaged for pressing a button the engine told them did nothing. **Fix:** add `resolved: boolean` to `RoundResult` and gate on it |
| G37 | **Two concurrent model loads on first run — a double 2.5 GB download** | `BUG` — **fix specified** | ⛔ | #14 | `electron/main.mjs:62` assigns `narrator` only *after* the await, so the guard is blind to an in-flight load. `createWindow` fires it un-awaited and the first `llm:generate` fires it again; both see `null`. **Measured: 2 concurrent `createNarrator()` calls where 1 is expected.** Reachable in normal play — the first generate lands on the stats-accept step, four clicks in, well inside the load window. **On first run that window is a ~2.5 GB download**, so two `resolveModelFile` calls race into the same directory, plus a second `getLlama()`/`loadModel` (VRAM OOM on a min-spec GPU). The loser's status callback also ends up bound to a different model instance. **Fix:** memoize the **promise**, not the result, and null it on rejection |
| G39 | **A flee consumable ignores `canFlee` — escapes any boss and PERMANENTLY soft-locks Act 5** | `BUG` — **fix specified** | ⛔ | #0a | `resolveUseConsumable` returns `'fled'` without checking `state.canFlee`, which every boss battle sets to `false`. **Measured:** `smoke-vial` in the Act-5 Hollow fight → `status: fled`, back to the hub at act 5. The Hollow is constructed **only** at `act-intro(5)` and `shouldAdvance` is false for act 5, so **the run is stuck with no path to any ending** — death or Quit only. ⚠ *Corrected 2026-08-30: this said "stuck **grinding random encounters**", but **G43** shows the act-5 hub is otherwise unreachable — so this soft-lock is currently **the only way to see floor-5 content at all**. Fixing G43 also removes this soft-lock, since a fled Hollow becomes re-fightable once the gate re-opens.* ⚠ **Honest reachability caveat:** no flee consumable is obtainable today (that is G14), so this goes live the moment G14 is fixed. It is a real defect through the engine API now. **Fix:** `if (res.fled && state.canFlee)`; otherwise consume the item and emit `escape-impossible` |
| G40 | **The debug overlay swallows backtick and F2 inside text inputs** | `BUG` — **fix specified** | medium | #0c | `debug-overlay.ts:53` binds a window-level `keydown` with no `ev.target` check, registered at module scope for the whole session. Typing `` ` `` in your character name eats the character **and** opens a panel covering 46% of the screen; typing `` ` `` in the overlay's own filter box closes the overlay instead of filtering. **Fix:** bail when the target is an `INPUT`, `TEXTAREA` or `contentEditable` |

### Found 2026-08-28 by discrepancy pass 8A — all six reproduced by running the real engine

| # | Item | Status | Severity | Blocks | Notes |
|---|---|---|---|---|---|
| G29 | **A THIRD unguarded damage site — Kingpin minion damage bypasses shield, revive and every `onTakeDamage` relic** | `BUG` — **fix specified** | ⛔⛔ | #0a | `boss.ts:305` writes `player.hp` directly and decides `player-died` itself, skipping all three gates `battle.ts` owns. **More reachable than the G24 site** — `BALANCE-REPORT.md` names the Act-1 Kingpin as the single biggest Act-1 killer, so **this is the death that happens most.** Reproduced: shield 20 absorbed **nothing**; Halo Fragment produced **no `revive` event** where the same relic fires correctly against ordinary damage; Mirror Shard reflected **nothing**. ⚠ **A FOURTH site exists too — `resolveUseConsumable:578`.** **Fix:** extract one `applyDamageToPlayer` helper (shield → `onTakeDamage` → revive) and call it from all four sites; return minion damage as a number rather than applying it in `boss.ts` |
| G30 | **Fracture inflicted on the ENEMY is completely inert** | `BUG` — **fix specified** | ⛔ | #0a | `battle.ts:211` consumes `etc.skipTurn` but **never reads `etc.advDisOverride`** for the enemy, though step 3 does read it for the player. So the enemy half of fracture has no consumer. **`heavyStrike` — the Enforcer's core skill — inflicts fracture.** Reproduced at seeds 3/9/21/44: fractured vs clean enemy give **byte-identical attacks** (`faces: [4]`, one die, `advDis: 0`). It also emits **no event** on its active tick, so the log shows nothing either: you cast Heavy Strike, see "fracture takes hold", and then nothing happens for 100 turns. **Fix:** thread `etc.advDisOverride` into the enemy's roll — pairs naturally with G12, its mirror-image on the player side |
| G31 | **The player's skill charges are NEVER restored — the skill system is one-shot per run** | `BUG` — **fix specified** | ⛔ | #0a | `resolveRestDecision` touches only `hp` and `restsLeft`. Reproduced: rest at `hp 3, charges 0` → `hp 12, charges 0`; a full 493-step Enforcer run with **6 rests taken** ends at **0 charges**, the only rises all run being two draft picks. **This contradicts a decided design** — `GAME-DESIGN.md` §18.1: *"A rest restores HP **and skill charges**… without it, long runs become charge-starved and classes stop playing like themselves in the back half of a run."* That is exactly what ships. Mirror of G22(c), which was recorded for the **enemy** side only. **Fix:** restore to `maxSkillCharges` on rest, including the `rest-full` early-return branch |
| G32 | **`Player.proficiency` is dead state, and M15 tuned the whole game against the wrong baseline** | `BUG` — ✅ **AUTHOR RULED 2026-08-30: WIRE IT, then re-run the balance sim** | ⛔ | #0a | Set at creation, validated by the save guard, and **read by no combat path** — `resolvePlayerAttack` builds to-hit from `weaponModifier` alone. Reproduced: 200 rounds at `proficiency: 2` vs `99`, same seeds → **0/200 differences.** So the player rolls at a flat **−2 to-hit** against the intended model (~10 percentage points of hit rate) for the entire game, and **M15 tuned enemy HP and damage against that unintended baseline.** `progression.ts:96` states *"proficiency is NOT auto-grown"*, which reads as "applied but static" — the code applies it never, so the doc lies about the rules. **✅ AUTHOR'S RULING (2026-08-30): WIRE IT.** Add `player.proficiency` to the to-hit total in `resolvePlayerAttack`, restoring the intended combat maths. **The balance re-run in #2 is therefore doubly mandatory** — this makes the player meaningfully more accurate, so M15's enemy-HP and damage constants were tuned against a baseline that no longer applies. *(Rejected: deleting the field, which would have made the shipped −2 the real design.)* |
| G33 | **The Cast picker ignores `chargeDiscount`, so charge-reduction relics deliver ZERO benefit** | `BUG` — **fix specified** | ⛔ | #0c | `view-model.ts:74` gates on `skillCharges >= chargeCost` while the engine gates on `chargeCost - discount`. A disabled button gets **no click handler at all**, so the action is genuinely unreachable, not merely mislabelled. Reproduced: Enforcer with Overclock Chip at 1 charge, `heavyStrike` cost 2 → **the engine resolves the cast normally**; the UI marks it unaffordable. **So Overclock Chip and Hollow Heart do nothing through the real UI at exactly the margin — the last charge — where a cost reduction is the entire point.** The sim dispatches straight to `step`, which is why M15 never saw it. **Fix:** compute the discount in `castOptions` and use it for both the hint and `affordable` |
| G34 | **`momentum` is the FOURTH multi-battle carry-over leak** | `BUG` — **fix specified** | high | #0a | Nothing zeroes it at battle end, and `game.ts` writes `battle.player` back to the hub. Reproduced: a won battle leaves `momentum: 5` (the cap) on the hub player, so an Enforcer opens **every** later fight — Kingpin, Reflection, Sin, Hollow — with full momentum banked, making the opening `heavyStrike` deal **8 instead of 3** against a 10-HP Act-1 enemy. Intra-fight that takes ~3 rounds to earn. Joins `shield` (G25), `activeConditions` (G27) and `advantageDisadvantage` (G12). ⚠ **Honest caveat:** unlike `shield`, `momentum` has no explicit "transient" statement in either the code or §4, so this is a design question the code answers silently. **Fix (if per-battle is intended):** zero it in `createBattle` — the single funnel every battle passes through, and the natural place to close **all four** leaks at once |

### Found 2026-08-28 by discrepancy pass 8B — SHIPPED STRINGS that contradict the locked fiction

> **These are content/data defects, not engine bugs.** They belong to the **§21.3 content pass /
> `PLAN.md` #13** — **except `C7`**, which rides with `G18` in `#0c` because its **`format.ts:74`**
> half is a `render/format.ts` change. ⚠ **Its `narrate.ts:47,49,51` half is LIVE TODAY** and is
> carried by **C10** (routed `#13 #12`), which also keeps `llm/narrate.ts` out of `#0c`'s territory.
> *(Corrected 2026-08-31: this clause used to say C7 "only becomes visible once G18 makes that file
> reachable" — the claim the C7 row itself retracts three lines below. Worse than a stale duplicate:
> here it was the load-bearing **reason** for a scheduling decision.)* Listed here because the register is where open items live, and
> because two of them are visible to the player in the first minute of a run.

| # | Item | Status | Severity | Blocks | Notes |
|---|---|---|---|---|---|
| C1 | **The two reserved words are used casually in shipped strings** | `BUG` — **fix specified** | ⛔ | #13 | `WORLD.md` §0 `[LOCKED]` is emphatic: *"Never use 'hollow' casually anywhere in the game's text. It is the load-bearing word. **Reserve it.**"* and *"made whole"* is *"the win condition of the entire game stated in one word"*, which *"no prose may soften"*. Violations shipped: `narrate.ts:83` *"you pry it open, but it is **hollow**"* (the load-bearing word spent on an empty box) and `narrate.ts:69` *"You are already **whole**"* — which tells the player they have **won** while the extraction is still running, so it is not merely off-register, it is **false in-fiction**. Also in data: `items.json` "Hollow Ring", `relics.json` "Hollow Heart", "Void Pact", `consumables.json` "Void Draught", and `enemyNames.json` generating floor-3 filler literally called **"Hollow Grief"** and floor-5 trash called **"Empty Void"**. *(`Hollow Regalia`, `The Hollowed` and `Hollow Self` are correct reserved uses — keep them.)* **⭐ Added by pass 12B:** the render layer's implementation of **the same event** gets it right — `format.ts:180` writes *"The cache is empty."* — so **the two paths for one event disagree, and only the narration path spends the word.** That is the fix, already written: use *"empty"*. ⚠ **The empty-chest branch is currently unreachable** (`dropTables.json` sets `chestItemCount: 1` with guaranteed items, so `loot.length` is never 0) — but `loot.ts` flags every value there as *"an M15 balance placeholder"*, so **one balance tweak makes it player-visible**, and the raw fact line prints verbatim whenever the model fails |
| C2 | **The class picker states the game's central concealed fact outright** | `BUG` — **fix specified** | ⛔ | #13 | `desktop/game.ts:107` labels the class *"Hollow — **the Void within**"*. `WORLD.md` §12 `[LOCKED]`: *"The prose must **never explain any of this**… the player should be able to finish the game without ever being told."* The label gives away *"the Void is a place within your mind"* **on the character-creation screen**, before the run starts. **Fix:** a label that does not assert the secret |
| C3 | **The game's only authored intro sends you to the wrong place** | `BUG` — **fix specified** | ⛔ | #13 | `story.json:10` — the Arch-Mage orders you *"to delve into **the Rift**"*. But `WORLD.md` §4 `[LOCKED]` puts the errand in the **Undercity** (*"Floor 1 is the last real place and this is a real errand"*), and §13 makes the Rift a separate stratum **below** it holding the Memorian facility. The betrayal scene turns on the Kingpin waiting *"at the very entrance of the Rift — **the last place you could still have turned around**"*. If the standing order was to enter the Rift, **crossing that threshold was the mission**, and the line the whole scene hangs on stops meaning anything. **Fix:** *"to go down into the Undercity"*. ⚠ `SCOPE-AUDIT.md` §2.9 asserts this block is *"now consistent with `WORLD.md`"* — it is not; corrected there too |
| C5 | **A shipped line says the floor-4 angels are a hallucination** ⭐ added 2026-08-30 | `BUG` — **fix specified** | ⛔ | #13 | `condition.ts:159` — `'~Angels underwater after the darkness~'` is one of 14 `INSANITY_STRINGS` pushed into the event stream **every turn** the player has `insanity`, on **any** floor, applied by six shipped skills. `WORLD.md` §6 `[LOCKED]`: *"Floor 4 — **the angels are REAL. They exist.** The condition does not manufacture them"* and *"floor 4 is the one floor that is not a lie."* `ART-BIBLE.md` rule 4: *"If the angels read as hallucination, **the art has broken the game's one hopeful fact.**"* This line does exactly that, in words rather than art — and can fire on **floor 1**, three floors before the player has any other frame for the word. **Fix:** delete it (legacy Java text, ported verbatim) |
| C6 | **Two different shipped items are both named "Clarity Draught"** | `BUG` — **fix specified** | medium | #13 | `items.json` `clarity-draught` (heal 8) and `consumables.json` `clarity-tonic` (cure `insanity` + apply `wise`) carry the **same display name** and load into the same catalog, so both can sit in one inventory under one name with different behaviour. `GAME-DESIGN.md` §6 documents only the second. **Fix:** rename or delete the `items.json` one; note the survivor's id/name mismatch too |
| C7 | **The combat log prints raw condition IDs, not display names** | `BUG` — **fix specified** | medium | #0c #13 | `render/format.ts:74` emits *"You cannot act — insanity."* and *"**dumb** takes hold of the enemy."* The display names exist in `CONDITION_DATA` and are simply not read. Compounds the two-vocabularies problem: the player sees the **code ids**, which is the vocabulary §14.4 ruled against. ⚠ **CORRECTED 2026-08-31 by pass 14A — this row understated the defect.** It said *"currently invisible because `format.ts` is dead code (G18), so this surfaces the moment G18 is fixed."* **That is wrong: the identical defect is LIVE TODAY** at `narrate.ts:47,49,51`, both inside every prompt the model receives **and** shown verbatim to the player on the model-failure path. Two ids also *differ* from their display names (`quick`→"Agile", `smart`→"Brainy"), so they would read wrong even after `format.ts` is revived. **Widened to name `narrate.ts:47,49,51` as well as `format.ts:74`** — see **C10** |
| C8 | **Four smaller shipped-text defects** | `BUG` — **fixes specified** | low | #13 | (a) `condition.ts:157` ships a typo in player-facing text: *"you can't stop laughing **histerically**"*. (b) **6 of the 14 insanity strings are the identical `"~Don't fall~"`**, so that one line shows ~43% of the time the condition ticks. (c) `enemyNames.json` `theHollowed` can generate **"The Husk Husk"** and **"The Hollow Hollowed"** — 2 of its 16 combinations — because `first` and `last` both carry the same words. (d) The `theHollowed` case also spends the reserved word "Hollow" on a generated trash name (see C1) |
| C9 | **The Inventory screen prints raw enum identifiers on 41% of all loot** ⭐ added 2026-08-31 | `BUG` — **fix specified** | medium | #13 | `view-model.ts:139` `describeItemEffect` has no prose for the `triggered` branch and returns `` `On ${effect.trigger}: ${effect.action.kind}` `` — so the hub Inventory screen renders **`On onHit: dealDamage`**, two internal enum members, verbatim. **Measured:** over 16 real seeded runs it is the single most common effect line (3,880 occurrences); a 400-seed sweep found **336 of 811 victory drops (41%)** carry it, because `rarityGen` gives every Legendary and half of Rares a `triggered onHit` proc. Distinct from the known generated-*name* defect — different file, different string, different fix. **Fix:** a small `{trigger, actionKind} → phrase` table beside the passive cases already handled below it |
| C10 | **`narrate.ts` ships `"You suffer(s) 1 bleed damage."`, and calls BUFFS afflictions** ⭐ added 2026-08-31 | `BUG` — **fix specified** | medium | #13 #12 | Three defects in two lines of `describeEvent`, all **live** — these fact lines are both the ground truth fed to the model in *every* prompt and the text shown verbatim to the player when the model fails. (a) **A literal placeholder plural ships:** `suffer(s)` — wrong for *both* subjects (*"You suffer(s)"*, *"The enemy suffer(s)"*). (b) **Buffs are described as afflictions:** `brace` (an Enforcer **core** skill) and `slip` (a Scavver **core** skill) apply self-conditions, so a defensive buff reads **"You are afflicted with healthy."** (c) Raw condition ids instead of display names — the live half of **C7**. **Measured over 24 real seeded runs:** 134 × *"The enemy is afflicted with exposed."*, 43 × *"You suffer(s) 1 bleed damage."*, 8 × *"You are afflicted with healthy."* **Fix:** conditional plural, `CONDITION_DATA[…].displayName`, and split `condition-applied` so buffs read *"steadies"/"quickens"* rather than *"afflicted"* |
| C11 | **Neither player-facing projector has a single test — the root cause of C9 and C10** | `BUG` — **fix specified** | medium | #13 | `describeItemEffect` appears in **zero** `.test.ts` files, and `view-model.test.ts` builds every fixture item with `effects: []` and hand-written pretty names (*"Whispering Band"*, *"Ashen Ring"*, *"Void Plate"*) that `rarityGen` **never produces** — so the tests describe an idealised catalogue that does not exist in the game. `narrate.test.ts` has 8 tests and **never emits a condition event of any kind.** ⚠ **This is why both defects survived thirteen audit rounds and 1029 green tests.** Same family as G11b: a suite that proves the code does what it was written to do, against fixtures chosen to match it. **Fix:** assert projector output against *real* `generateItem` and real engine events, not hand-built fixtures |
| C12 | **The shipped ending anchors are third person and name the player — the author ruled them WRONG** ⭐ added 2026-08-31 | `BUG` — **fix specified** | high | #13 | `story.json` ships *"{playerName} is judged worthy and rises from the Void, made whole"* and *"{playerName} is cast down…"*. **§22.1 ruled the endings speak in SECOND PERSON and never by name.** `WORLD.md` §9 already renders both correctly — *"you come back, and you are changed"* / *"You failed the only door out"* — so the prose exists; it is the data that is wrong. ⚠ **These are the last words of a completed run**, and they currently break the voice rule at the one moment it matters most. *(Logged as work because closing A8 answered the question but did not change the file.)* **Widened 2026-08-31 — the anchor map:** `story.json:36` (grace) renders via `game.ts:444`; `:40` (damnation) via `game.ts:341`; `:31` (the legacy `END.` anchor) has **no live call site** — test-only. **The engine-side name leak is separate: G47** |
| C4 | **The narrator is never told where it is** | `BUG` — **fix specified** | medium | #12 #13 | `narrate.ts:14` feeds the model `'the First Floor' … 'the Fifth Floor'` — the real names (`Undercity`, `Entrance to the Void`, `Ash City`, `Angelic Underground`, `True Void`) exist in `render/tokens.ts` and are never passed. `UI-DESIGN.md` §11 `[DECIDED]` requires *"a narration beat on every descent — the narrator states plainly"* which floor you are on; **it cannot name a place it was never given.** The `?? 'the Void'` fallback also names the Void as a *location*, against `WORLD.md` §6: *"**The Void is not a place.**"* **Fix:** pass `FLOOR_THEMES[place].name`; fall back to *"somewhere further down"* |

### Found 2026-08-28 by discrepancy pass 7A — all six verified by running the real engine

| # | Item | Status | Severity | Blocks | Notes |
|---|---|---|---|---|---|
| G23 | **Re-applying a damage-over-time condition resets it to ONSET, so it deals ZERO damage forever** | `BUG` — **fix specified** | ⛔⛔ **core combat** | #0a | The onset phase is *inferred* from `remainingTurns === maxTurns` (`condition.ts:304`), but the refresh branches (`:203`, `:206`) write **exactly that value**. So every re-application drops a live condition back into its no-effect onset turn. **Measured: spamming `ember` for 20 rounds deals a total of 0 burn damage**, ending `{burn, rem:2, max:2, intensity:21}` — while casting it **once** deals 1. The dominant player action is strictly worse than acting once. Affects every `bleed`/`burn`/`poison` skill **on both sides**, and means a refreshed `freeze`/`stun` never reaches the active branch so **its saving throw is never rolled**. **Fix:** stop overloading `remainingTurns` as the phase flag — add an explicit `onsetDone` flag, or have refresh write `Math.max(maxTurns - 1, 1)` |
| G24 | **The failed-escape counter-attack bypasses the ENTIRE defensive pipeline** | `BUG` — **fix specified** | ⛔ | #0a | `enemyCounterAttack` (`battle.ts:603`) subtracts HP raw and checks death raw, skipping all five guards `resolvePlayerTurn` applies: shield absorb, `firstHitReduction`, the `onTakeDamage` triggers, the **revive gate**, and the transient-flag threading. `resolveUseConsumable` misses the revive gate too. **All three measured:** Grace-Forged Aegis shield **not consulted** (HP drops, shield untouched); **Halo Fragment does not revive you** — status `player-died` where a normal round revives; Scrap Plating's free hit is consumed *and* still banked. So the once-per-battle revive relic fails at the death players most often walk into. **Fix:** extract the damage-application block into one helper and call it from all three sites — every step is RNG-free, so draw order is unchanged |
| G25 | **`player.shield` is never cleared at battle end — it accumulates without bound** | `BUG` — **fix specified** | ⛔ | #0a | `Player.shield` is documented as *"transient combat shield"*, but the only writes are `+= amt` at battle open and `-= absorbed` on a hit, and `game.ts` writes `battle.player` back to `state.player` on every outcome. `openBattle` re-fires the `startOfBattle` trigger against the carried-forward player. **Measured with Grace-Forged Aegis over five battles: shield at start = 5, 10, 15, 20, 25** — and it keeps climbing all run, and **survives the save file**. By mid-descent the player is effectively immune to chip damage. **Fix:** strip transient fields when writing `battle.player` back, or reset `shield` in `openBattle` before firing the trigger |
| G26 | **When the model fails, the fallback prints the PREVIOUS beats instead of what just happened** | `BUG` — **fix specified** | ⛔ | #0c | `desktop/game.ts:202` takes `prompt.user.split('\n\n')[0]`, assuming the facts block is the first paragraph. It is — **only when memory is empty.** From the second narratable turn of every run onward, the first paragraph is the *continuity recap*. **Measured:** after a crit, the fallback renders "Across this descent you have felled 1 foe / Recent moments…" and **never the words describing the crit**. Since a GPU is the stated min spec and this is the *designed* degraded path, a player on a marginal machine sees a stale recap after every action and is never told what the engine did — the opposite of the comment's claim that it "remains fully playable". **Fix:** return the already-computed `facts` from `buildNarrationPrompt` and render those, instead of re-deriving by string-splitting |
| G27 | **Fracture is effectively PERMANENT — rest does not cure it and the only cure is unobtainable** | `BUG` — **fix specified** | ⛔ | #0a | `condition.ts:95` sets `maxTurns: 100` with the comment *"needs a rest"* — but `resolveRestDecision` only restores HP and decrements `restsLeft`; **it never touches `activeConditions`.** The only cure in the data is `warding-charm`, which is a battle-only action **and** unreachable by any loot path (G14). **Measured:** a floor-1 Ganger's `gangStomp` applies fracture; it is still active after **99 rounds**, setting attack disadvantage every one of them, and rides into every later battle via the write-back. **One first-floor enemy skill puts the player on permanent attack disadvantage for the whole run with no in-game remedy.** **Fix:** clear conditions in `resolveRestDecision` — the behaviour the 100-turn duration was tuned for |
| G28 | **Five smaller verified defects** | `BUG` — **fixes specified** | medium | #0a #0c | (a) **The HUD shows no condition chips at all** — the whole `components.ts`/`component-model.ts` library is unimported by the real UI, so a stunned, poisoned or fractured player has **no on-screen indication**. Same family as G18. (b) `renderSheet` interpolates the **player-entered name into `innerHTML`** (`game.ts:157`) while loot rows correctly set text — a name containing `<` corrupts the sheet markup. (c) `scripts/balance-report.ts:162` mixes live numbers with **hard-coded claims** ("Acts 1–4 each hold ≥10% of deaths", "Scavver is strongest") — after any retune the report asserts falsehoods with authority; `actsWithShare()` already exists and is used correctly 44 lines above. (d) `items.json` `clarity-draught` is `kind: "usable"` with **no `use` array**, so it can never be used while still advertising "Heal 8 HP". (e) `view-model.ts:331` calls `resolveSkill` with **no null guard** where its sibling at `:68` does — an unknown skill id from a hand-edited save crashes the character sheet instead of skipping the row |

### Found 2026-08-28 by discrepancy pass 6A — all six verified by running the real engine

| # | Item | Status | Severity | Blocks | Notes |
|---|---|---|---|---|---|
| G17 | **The ENTIRE resistance subsystem is inert** | `BUG` — **fix specified** | ⛔ |  #0a  | Two compounding breaks. (a) `skill.ts:307` computes `Math.floor(res / 100) * baseDamage` — **zero for every value below 100**, and nothing in the game produces ≥100 (`blessed.resistBonus = 2`, family `resistAmount: 2`, `RESIST_PER_WIS_MOD = 10`). Verified: damage is **3 for res ∈ {0, 2, 10, 20, 50, 99}**. (b) `effectiveResistances` — which layers the Lucid/Clouded WIS shift and equipped-gear `bonusResist` — **has zero production callers**; both damage paths read the raw stored array. Dead as a result: 7 elements, 5 family themes, the `blessed` affix (a "Blessed Ganger" is mechanically an ordinary Ganger), the `bonusResist` item-effect, and 2 conditions. **Fix:** real percentage formula + call `effectiveResistances` at both sites; then rebalance the data values, which only become meaningful once the formula works |
| G18 | **The player never sees a single damage number, die, hit, miss or crit** | `BUG` — **fix specified** | ⛔ |  #0c  | `src/render/format.ts` (205 lines of `formatEvent`, plus `formatRollDetail` built for `UI-DESIGN.md` §3) **is imported by nothing but its own test.** `desktop/game.ts` imports only `theme`, `component-model` and `components` from `render/`. So the entire M-UI2 roll-detail pipeline — `AttackRollDetail`, `faces`, `damageSources`, `foldDamageSource`, the `crit-dice`/`clamp`/`damage-mult` terms — is **computed on every attack and consumed by nobody**. The one channel that does render is the narration, and `VOID_PERSONA` explicitly forbids it from mentioning *"game mechanics, dice, or numbers"*. **Also unused:** `panel`, `bar`, `conditionChip`. **Fix:** add a log element to `desktop.html` and append `formatEvent(e)` in `dispatch()` |
| G19 | **Resuming a save silently forfeits meta-progression** — ⭐ **this is the root cause of G1** | `BUG` — **fix specified** | ⛔ |  #0c  | `runSummary` is a derived accumulator and the sole input to `applyRunSummary` (which grants every feat and unlock). `persist.ts` stores only `{state, memory}`, and the resume branch never rebuilds it — so a resumed run starts with `bossKills: []`, `spareCount: 0`, `maxAct: 0`. **Verified on seed 4242:** uninterrupted run unlocks Neuromancer; the *identical* run resumed from its own Act-2 state loses the `kingpin` boss kill and **unlocks nothing**. Resume late enough and every `reach-act-N` feat is forfeited too. `runSeed` is also re-stamped from `Date.now()`, so the recorded `runId` no longer identifies the run. **Fix:** persist `runSummary` + `runSeed` in the envelope and restore them; both are already plain data, so it is a field addition plus a version bump |
| G20 | **A sacrifice-deal can drive `maxHp` and `hp` NEGATIVE** | `BUG` — **fix specified** | ⛔ |  #0a  | `deal.ts:151` `canAfford` guards the `hp` cost (`cost.amount < player.hp`) but lets `maxHp` fall through to `return true`, and `applyDeal` subtracts with **no floor** — unlike every other max-HP sink (`classKit.ts:278` uses `Math.max(..., 1)`). **Verified:** `deals.json` `tempting` has a `maxHp: 6` cost; with `maxHp: 5, hp: 5` the deal is affordable and yields **`maxHp: -1`, `hp: -1`**. The player walks to the hub at −1 HP and dies on round 1 without acting; potions are refused and revive heals to 1. **Fix:** add the `maxHp` guard and floor at 1. The `statPoint` cost is unbounded the same way — clamp in the same edit |
| G21 | **Every Act transition renders a completely blank screen** | `BUG` — **fix specified** | ⛔ |  #0b  | Distinct root cause from G13: here the `describeEvent` cases *exist* but return `e.body`, discarding `e.header` — and **all ten** `actIntros`/`actOutros` bodies in `story.json` are `""`. So the prompt is `null` and the pane is cleared. **Measured over 20 full runs (8,654 steps): 47 `act-intro` + 47 `act-outro` blank screens**, one on every floor change — the five-Act descent is never announced anywhere in the UI. **Fix:** return `[e.header, e.body].filter(Boolean).join(' — ')` for `act-intro`/`act-outro`/`ending`, and author the ten bodies |
| G22 | **Three smaller verified defects** | `BUG` — **fixes specified** | medium |  #0a  | (a) `battle.ts:252` adds `ptc.hpDelta` with **no `maxHp` clamp**, so a `regeneration` tick or Penitent `consecrate` leaves `hp > maxHp` (reproduced). (b) `combat.ts:315` gates enemy skills on `skillCharges > 0` while `useSkill` subtracts the full `chargeCost` — an enemy with 1 charge casts a cost-2 skill and ends at **−1** (reproduced; affects `wrathSmash`, `riotSlam`, `overload`, `immovableSlam`, `smiteWicked`). (c) Enemies start at `skillCharges: 2` with **no restore path**, so a family's themed skill pool only matters for the first two landed hits and every later enemy hit is the flat `{kind:'base', amount:1}` |

## 5. Document hygiene

| # | Item | Status |
|---|---|---|
| D1 | ~~Asset totals in circulation~~ | `FIXED` 2026-08-27 — reconciled to **50 buildable / 52 eventual** (§4b); the double-counted Sins were the cause |
| D2 | ~~two §14s~~ — it was **two `## 16`s** (fixed 2026-08-27: the second is now `## 16b`, with numbering notes at §14 and §16b). §15 still says "the brainstorm is essentially complete" | `PARTLY FIXED` |
| D3 | **Two** live `[PROPOSAL]` tags to retag or delete: **P3** (`UI-DESIGN.md` decomposition) and **P4** (`UI-DESIGN.md` pipeline shape). *(Corrected 2026-08-28: this said "three — P2, P3, P5". **P2 and P5 no longer exist**; P1 is struck and closed; and **P4 was live and untracked** — the one the list missed.)* | `OPEN` |
| D4 | ~~`.env.example` says "bundled"~~ | `FIXED` 2026-08-26 |
| D5 | ~~`PROGRESS.md` still describes the shell as "mobile-first, portrait 540×1080"~~ | **`CLOSED` 2026-08-30 — the quoted text does not exist.** `grep "mobile-first" PROGRESS.md` returns nothing. The only `540` is inside a dated 2026-08-01 session-log entry that says **"responsive"** — the *current* doctrine, not the superseded one — about a Kaplay shell that has since been deleted. The session log is append-only history by its own convention, so there is nothing to action |
| D6 | `HUMAN-CHECKS.md` — run instructions **FIXED 2026-08-27**; carries **10** un-ticked author rulings (not 5 — that figure was copied from the frozen `SCOPE-AUDIT-2.md` snapshot), of which **3 were already answered by merged milestones** (enemies-always-hit → M4; Beast-only → M8; shop-under-Character-Info → M7 deleted the shop). **Those 3 struck 2026-08-28; 7 genuinely remain**, incl. flee 25% vs 35% | `PARTLY FIXED` |
| D7 | **20** stale worktrees on disk (`PROGRESS.md` says 16; both were wrong) | `OPEN` |
| D8 | `WHAT-WE-BUILT.md` + `itch-description.html` are stale. **Widened 2026-08-30 — `WHAT-WE-BUILT.md` is wrong about SEVEN things, not three.** Four of the seven are decisions taken *after* it — gold/shop/Void-as-place, **a no-GPU min spec, a bundled model, LLM-authored choices, and a closing line saying the whole project is unmerged.** A dated superseded banner now heads the file. *(`itch-description.html` never mentions gold — that part of this row was wrong.)* **`HUMAN-CHECKS.md` did too** — its play-check scripted "rest → shop → level-up" (**fixed 2026-08-28**); it is the one a human actually follows, so it mattered most | `PARTLY FIXED` |
| D9 | **The BALANCE-REPORT invalidation banner lives in a GENERATED file and will be silently destroyed** ⭐ added 2026-08-30. `docs/BALANCE-REPORT.md` is generated by `scripts/balance-report.ts`, and its own header says *"Regenerate after any balance change."* The ⚠ INVALIDATED banner (G11 + G32) was hand-added to the **output**, and the generator does not emit it — verified: zero matches for `INVALIDATED` in `scripts/balance-report.ts`. **So the next regeneration wipes the warning**, and the report silently returns to asserting the "lower bound, real play is easier" claim that G11 falsified. That regeneration is *mandatory* after `#0` (twice over — G11 and G32), so this is not hypothetical. **Fix:** emit the provenance/caveat block **from the generator**, so it cannot be lost — or have the script refuse to overwrite a file containing an INVALIDATED banner without `--force`. ⭐ **CONFIRMED EMPIRICALLY by pass 13A** (2026-08-30) — it ran `npx vite-node scripts/balance-report.ts` against a backup and diffed: the numbers regenerate byte-identical, and **the only difference is the deleted ⚠ INVALIDATED banner.** No longer inferred | `OPEN` |
| D10 | **The CODEX was `[DECIDED]` twice and named in NO work item** ⭐ added 2026-08-31. `UI-DESIGN.md` §14 commits to *"a codex that fills in as you go"* and flags it **"⚠ net-new state, not a reuse"**; `GAME-DESIGN.md` §21.6 decides its content split. But `grep -i codex docs/PLAN.md` returned **zero** — unlike the settings screen and the content warning, neither section routed itself into a unit. **Net-new persistent state plus a screen is not a footnote to a restyle.** Now named in `PLAN.md` #8, but ~~its scope needs an explicit decision: fold in, or split out~~ **— mooted 2026-08-31: the codex is CUT (§22.11)**. *(The **sixth** propagation failure of this shape — the class rule 5 exists to prevent. Surfaced by round 14B, which correctly declined to file it as a fiction finding.)* | **`CLOSED` 2026-08-31 — cut by §22.11; no scoping decision needed** |

---

## Resolved

_Struck items stay here permanently. Never delete a row._

- ~~Socket contradiction — two `[LOCKED]` statements negating each other~~ → **DECIDED**: no visible
  hardware in any art; `ART-BIBLE.md` §9. Neuromancer costume corrected. ⚠ **Only TWO of the three
  legs were fixed on 2026-08-25** — `WORLD.md` §12c's socket bullet kept a contradicting premise and
  a false quotation of the retired costume text, with only its `[OPEN]` tag flipped to `[CLOSED]`.
  **Third leg corrected 2026-08-30**; this row's "corrected" is what made it look fully settled.
- ~~`CLAUDE.md` stale in seven places while holding top precedence~~ → **FIXED** 2026-08-25.
- ~~Economy contradiction: shops vs no-currency in the same document~~ → **FIXED**, §11 won.
- ~~Alpha strategy~~ → **DECIDED**: key flat black to PNG in post.
- ~~Affix visual treatment~~ → **DECIDED**: code effects, zero new art.
- ~~Backdrops 1K vs 2K~~ → **DECIDED**: everything 1K.
- ~~Floor mechanics architecture + all five floors~~ → **DECIDED**: `GAME-DESIGN.md` §8.
- ~~Who writes the choices~~ → **DECIDED**: the engine. M11 shrinks.
- ~~Karma's mid-run effects~~ → **DECIDED**: bends the world, never the numbers.
- ~~Relic acquisition~~ → **DECIDED**: sacrifice-deals only.
- ~~Model bundling~~ → **DECIDED**: download once on first run.
- ~~Audio scope~~ → **DECIDED**: effects + ambience + score (licence still `OPEN`, see S3).
- ~~Rival houses, Absolution above ground~~ → **PARKED** deliberately; `WORLD.md` §12b.
- ~~Content warning policy~~ → **DECIDED** 2026-08-26: every fresh run; `docs/CONTENT-WARNING.md`.
- ~~Colour as the only state channel~~ → **DECIDED**: floor-name tag + narration beat; `UI-DESIGN.md` §11.
- ~~Settings screen~~ → **DECIDED**: added to #8; `UI-DESIGN.md` §12.
- ~~Difficulty modes~~ → **DECIDED**: one difficulty, no assists; `GAME-DESIGN.md` §14.5.
- ~~Should floor names surface in the UI?~~ → **DECIDED**: yes, render them (falls out of §11).
- ~~Reduced motion~~ → **DECIDED** 2026-08-26: one OS-aware setting; `UI-DESIGN.md` §13.
- ~~Hands / dual-wield~~ → **DECIDED**: three configurations; `GAME-DESIGN.md` §14.6.
- ~~Backpack capacity~~ → **DECIDED**: fixed slots, no weight; `GAME-DESIGN.md` §14.6.
- ~~Equipment slot set~~ → **DECIDED**: seven slots, two trinkets; `GAME-DESIGN.md` §14.7.
- ~~Typeface~~ → **DECIDED**: JetBrains Mono, OFL.
- ~~Initiative~~ → **DECIDED**: none; fixed round order; `GAME-DESIGN.md` §14.8.
- ~~Model tier~~ → **DECIDED**: 4B only, **min spec now requires a GPU**.
- ~~Consumable/unique sources~~ → **DECIDED**; `GAME-DESIGN.md` §14.8.
- ~~Onboarding, saves, errors, window, input~~ → **DECIDED** 2026-08-26; `UI-DESIGN.md` §14.
- ~~Licence, updates, pricing, telemetry, privacy, age rating~~ → **DECIDED**; `docs/SHIPPING.md`.
- ~~Fleeing bosses, death screen~~ → **DECIDED**; `GAME-DESIGN.md` §14.9.
- ~~Accessibility (full, incl. screen reader)~~ → **DECIDED**; `UI-DESIGN.md` §15.
- ~~Ending text~~ → **DECIDED**: authored anchors + narrated specifics; `GAME-DESIGN.md` §14.10.
- ~~Item icons~~ → ~~**DECIDED**: bespoke per item~~ → **⚠ REVERSED 2026-08-31 (§22.8): none ship.**
- ~~External playtesting~~ → **DECIDED**: a small trusted group; `ART-BIBLE.md` §14.
- ~~Interface furniture~~ → **DECIDED** via the art/interface rule; `ART-BIBLE.md` §15.
- ~~Performance / quality gating~~ → **DECIDED**: 30fps, no degradation; `UI-DESIGN.md` §16.
- ~~Run length~~ → **DECIDED**: measure and tune in the balance re-run; `GAME-DESIGN.md` §14.11.
- ~~Quick/Slow effects~~ → **DECIDED**: a **tempo gauge** driven by DEX, visible on both combatants; `GAME-DESIGN.md` §16.1.
- ~~Mid-battle equipping~~ → **DECIDED**: hub-only; `GAME-DESIGN.md` §16.2.
- ~~Art running ahead of the engine~~ → **DECIDED**: no — engine first. Ash-Wretch and the Warden executioner drop out of the art list until they exist in code.
